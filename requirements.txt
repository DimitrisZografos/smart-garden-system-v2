# Smart Garden System Requirements

# Core dependencies
PyYAML==6.0
requests==2.28.1
Flask==2.2.3
Pillow==9.4.0

# Hardware interfaces (only needed on Raspberry Pi)
RPi.GPIO==0.7.1; platform_machine == 'armv7l'
picamera==1.13; platform_machine == 'armv7l'